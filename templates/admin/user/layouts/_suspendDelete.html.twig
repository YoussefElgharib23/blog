{{ include('client/shared/_style.html.twig') }}
<div class="container">
    <h3 class="text-center my-3 text-muted">{{ action ?? 'Suspend'}} account {{ user.fullName }}</h3>
    <div class="row">
        <div class="col-md-6 mx-auto">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <input type="submit" value="Send" class="btn btn-outline-success btn-block">
            {{ form_end(form) }}
        </div>
    </div>
</div>